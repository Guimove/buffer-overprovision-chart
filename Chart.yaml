apiVersion: v2
name: buffer-overprovision
description: Buffer / over‑provisioning chart for Karpenter – keeps low‑priority pause pods to maintain warm capacity
type: application
version: 0.1.0
appVersion: "1.0"
